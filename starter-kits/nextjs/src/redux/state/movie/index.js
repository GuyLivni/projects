import reducer from './movieReducer';
import * as movieActions from './movieActions';
import * as movieTypes from './movieTypes';
import * as movieSelectors from './movieSelectors';

export { movieActions, movieTypes, movieSelectors };

export default reducer;
