const KEY = {
  LIFECYCLE: "api-middleware/LIFECYCLE",
  TRANSACTION: "api-middleware/TRANSACTION"
};

const LIFECYCLE = {
  START: "start",
  SUCCESS: "success",
  FAILURE: "failure"
};

module.exports = {
  KEY,
  LIFECYCLE
};
